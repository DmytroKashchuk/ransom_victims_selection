# 5-swdb_prob_wrong folder is old test where i used DUPLICATED DATA from spicework db, during the filtering
#  selected distinct(site_name, site_url), and it was wrong because there was different site_names for the same site_id, such as: subway.com or statefarm.com

# 5-swdb_prob_right is the correct one, where i used distinct(site_url).





